{
  "method": "poc.policy.create",
  "params": {
    "policyName": "protect files",
    "template": {
      "kind": "prevent_file_deletion",
      "files": [
        "/workspace/important/**",
        "/etc/agent/config.yaml"
      ]
    }
  }
}
